# Search Patterns (Fast Grep Keys)

Use these strings/regexes to jump to the right place inside the reference library.

## Core building blocks

- `stack(`
- `arrange(`
- `setcpm(`
- `.slow(` / `.fast(`
- `sound(` / `s(`
- `note(` / `n(`
- `.scale(`

## Mini-notation features

- `bd(` (Euclidean kick)
- `hh(5,8` (Euclidean hats)
- `[` / `]` (sub-sequences)
- `<` / `>` (alternation)
- `*` and `/` (density)
- `@` (duration)
- `,` (parallel)

## Composition process (Anchor Framework)

- `anchor on steps 1,4,7,10`
- `Step 0:`
- `Step 1:`
- `Step 2:`
- `syncopation`
- `.room(`
- `.shape(`

## Pattern transforms / conditionals

- `.off(`
- `.layer(`
- `.superimpose(`
- `.echo(` / `.echoWith(`
- `.sometimes(` / `.often(` / `.rarely(` / `.sometimesBy(`
- `.rev(` / `rev()`
- `.jux(`

## Harmony / chords

- `chord("<` (chord progressions)
- `.voicing(` / `.voicings(`
- `.rootNotes(`
- `addVoicings(`

## Sampling

- `samples(`
- `.fit(`
- `.chop(`
- `.slice(`
- `.splice(`
- `.loop(` / `.loopAt(`
- `.begin(` / `.end(`
